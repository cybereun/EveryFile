# EveryFile 앱 업그레이드 계획서

작성일: 2026-09-06 · 상태: 구현 전 제안

## 1. 제안의 핵심

목표는 **수백만 개 파일을 다루더라도 목록부터 빠르게 보고 검색할 수 있는 앱**이다. 구글 드라이브 연결 시 파일 이름·종류·위치 등 기본 정보를 먼저 수집하고, PDF 파싱·OCR·본문 검색 색인·AI 분석은 별도 작업으로 분리한다. 화면 개편과 함께 느린 수집, 목록 누락, 재시작, 오류 복구까지 개선한다.

이번 요청의 산출물은 계획서이며 앱 코드를 수정하거나 현재 실행 중인 수집 작업을 중단하는 것은 포함하지 않는다.

### 조사 범위와 확실성

- 사용자 보고: 구글 드라이브 선택 후 목록 파악이 매우 느리며, 3일이 지나도 진행률이 2%에 못 미친다.
- 소스 확인: 현재 저장소는 EveryFile 1.0.4, React 19·TypeScript·Tauri 2·Rust 기반이다. SQLCipher를 사용하는 SQLite/FTS5와 별도 파서·OCR 프로세스가 이미 있다. 구글 드라이브 처리는 로컬 가상 드라이브를 고려한 폴더 탐색 코드이며, 확인한 `src`·`src-tauri/src`에는 Drive API/OAuth 커넥터가 없다.
- 로컬 도구가 처음에 지연됐지만 복구 후 주요 소스·마이그레이션·UI 코드를 검토했다. 사용자가 실행 중인 바이너리와 이 소스의 일치 여부, 실제 DB·로그·화면·성능은 아직 확인하지 않았다. 따라서 아래의 코드 사실과 실측이 필요한 병목 가설을 구분한다.
- Google Drive API의 목록 조회, 변경 추적, 성능 및 사용량 제한은 공식 문서를 확인했다. 제안 구조와 목표 수치는 설계안이며 달성 실적이 아니다.

## 2. 가장 먼저 확인하고 고칠 문제

### 2.1 소스에서 확인한 사실과 구체적인 수정 대상

| 확인한 코드 | 현재 동작과 의미 | 제안 변경 |
|---|---|---|
| `src-tauri/src/folders/discovery.rs`의 `discover_path`, `walk_root` | Google Drive 가상 경로를 폴더 순회하며 채널 크기는 32다. 전체 폴더 정렬을 피하고 placeholder는 메타데이터만 처리하는 보호가 이미 있다. | 보호 로직 유지. API 커넥터 추가와 로컬 목록 전용 모드 제공. 클라우드 하위 폴더의 reparse 속성이 제외 규칙에 걸리는지 실계정 검증. |
| `src-tauri/src/indexing/coordinator.rs:1208`의 `run_discovery`와 `persist_discovered_candidates` | 32건 단위로 파일명과 작업을 DB에 먼저 저장한다. 처음부터 PDF를 모두 처리해야 파일명이 저장되는 구조는 아니다. | 첫 목록은 건수 또는 시간 기준으로 빠르게 flush하고, 이후 저장 묶음 크기는 계측해 조정. 파싱을 끄는 명시적 목록 모드 추가. |
| 같은 파일 `run_job` 및 `persist_discovered_candidates` | 첫 묶음 저장 후 탐색 완료 전에도 `parsing` 상태로 전환하고 파서를 최대 3개 병행한다. | 목록과 콘텐츠의 상태·자원 예산을 독립화. 목록 우선 정책을 적용하고 콘텐츠 작업은 별도 일시정지 가능하게 변경. |
| `src/features/folders/IndexStatus.tsx:37` | `discovering`일 때만 백분율을 숨긴다. `parsing` 전환 이후에는 계속 늘어나는 `totalFiles`를 분모로 사용한다. | `discovery_complete`와 발견·목록저장·분석완료 건수를 별도 IPC 필드로 전달. 2%를 전체 목록 진행률로 오해하지 않게 수정. |
| `src-tauri/src/infrastructure/database.rs:28` | WAL을 사용하지만 앱 내부는 `Mutex<Option<Connection>>` 하나를 공유한다. 검색·목록 저장·파싱 결과 저장이 연결 잠금에서 경쟁할 수 있다. | 암호화 설정을 유지한 읽기 연결 분리와 짧은 쓰기 트랜잭션을 검토. WAL 활성화만으로 앱 내부 직렬화가 풀리지는 않음. |
| `src-tauri/migrations/0001_initial.sql:34`, `coordinator.rs:2726` 부근 | FTS5의 `document_id`는 `UNINDEXED`인데 발견 파일마다 해당 ID의 존재 여부를 조회한다. ID 기반 UPDATE/DELETE에도 같은 패턴이 있다. | 우선 `EXPLAIN QUERY PLAN`과 증가 규모별 시간을 측정. 선형 순회가 확인되면 전체 수집 비용이 급증할 수 있으므로 정수 ID↔FTS `rowid` 매핑으로 직접 접근하도록 마이그레이션. FTS 가상 테이블의 일반 컬럼에 B-tree 인덱스를 단순 추가하는 방안은 사용하지 않음. |
| `coordinator.rs:922`의 `stored_path_for_event` | 이벤트 경로를 찾기 위해 해당 폴더의 저장 경로를 모두 순회한다. | 정규화한 경로 키에 인덱스를 부여해 직접 조회. 대규모 이벤트 폭주 시 반복 전체 조회 방지. |
| `coordinator.rs:959`의 `reconcile_blocking`, `indexing/watcher.rs` | 재대조 경로는 변경 후보마다 `run_job` 완료를 기다린다. watcher에는 15분 주기 재대조 경로도 있다. | 재대조에서도 메타데이터 갱신과 콘텐츠 분석 분리. 가상 드라이브 재탐색 빈도·중복 실행·변경 감시 실패 시 동작을 점검. |
| `src-tauri/src/search/repository.rs:65` | 매 검색에서 정확한 COUNT를 먼저 계산하고 LIMIT/OFFSET으로 결과를 가져온다. 파일명 검색에는 LOWER/LIKE 경로가 있다. | 첫 결과를 우선 반환하고 정확한 합계는 별도 계산·캐시. 커서 조회와 부분 이름 검색용 색인 검토. |
| `src/features/search/useImmediateSearch.ts:163`, `SearchResults.tsx:471` | 더 보기 결과를 배열에 누적하고 받은 행을 모두 렌더링한다. 검색 조건이 없으면 결과를 비운다. | 별도 ‘전체 파일’ 탐색 모드를 추가하고 가상 목록·페이지 캐시 상한 적용. 기존 검색 취소와 최신 응답 판별은 유지. |
| `src-tauri/src/lib.rs:58` | DB와 진단 로그가 `app_local_data_dir`에 생성된다. 앱 실행 파일을 L드라이브에 두는 것만으로 앱 데이터도 L에 저장되지는 않는다. | DB·로그·미리보기 캐시 위치를 선택 가능하게 하고 사용자 환경의 기본 제안을 L드라이브로 설정. |

**가장 먼저 검증할 병목은 FTS ID 조회, 공유 DB 잠금, 가상 드라이브 순회, 재대조 중 파싱 대기다.** PDF가 단독 원인이라고 단정할 근거는 없다. 특히 새 파일 목록은 이미 먼저 저장되므로, 기존 설계를 보존하면서 실제 확장성 결함을 고치는 접근이 필요하다.

### 2.2 추가 진단 체크리스트

다음 표는 모든 경로에 대해 아직 확정하지 않은 점검 항목이다. 위 표에서 이미 확인한 배치 저장·placeholder 보호·파서 분리 프로세스·복구 기능을 없는 기능처럼 다시 구현하지 않는다.

| 우선순위 | 문제 후보 | 확인 방법 | 수정 방향 / 완료 기준 |
|---|---|---|---|
| P0 | 목록 루프 안에서 다운로드·PDF 파싱·OCR를 기다림 | 목록 조회부터 DB 저장·화면 반영까지 호출 흐름과 소요 시간 추적 | 목록만 모드에서 콘텐츠 다운로드·파서 호출 0회 |
| P0 | 가상 드라이브 파일을 열어 클라우드 다운로드 유발 | 선택 경로, 파일 읽기·해시·미리보기 호출, 네트워크 바이트 확인 | 클라우드는 API 메타데이터 수집 경로 제공; 로컬 탐색도 콘텐츠 읽기 분리 |
| P0 | 페이지 일부만 읽거나 공유 드라이브·폴더를 누락 | 페이지 토큰, 조회 범위, 권한, 필터, 중복 제거 키 확인 | 마지막 페이지까지 처리하고 범위별 완료 여부 검증 |
| P0 | 파일마다 DB 커밋·UI 전체 갱신 또는 반복 상세 조회 | 파일당 API 호출 수, DB 쓰기 시간, UI 이벤트 수 측정 | 페이지 단위 묶음 저장, UI 갱신 빈도 제한, 목록 응답 재사용 |
| P0 | 실패 파일 무한 재시도, 중단 후 처음부터 시작 | 재시도 기록, 체크포인트, 재실행 비교 | 제한된 재시도, 실패 격리, 저장 지점부터 복구 |
| P1 | 전체 건수를 모르는 상태에서 잘못된 백분율 표시 | 진행률 분모와 단계별 카운터 확인 | 목록 수집과 본문 분석을 분리하고 확인된 건수 표시 |
| P1 | 대량 목록을 메모리에 모두 올려 UI 정지 | 10만·100만 건에서 메모리·검색·스크롤 측정 | DB 조회 기반 목록과 화면에 보이는 행만 렌더링 |
| P1 | 파일 이동·이름 변경·접근권한 변경 시 오래된 목록 잔존 | 변경 이벤트와 로컬 DB 대조 | ID 기반 갱신, 삭제·권한 상실 처리, 변경분 동기화 |

진단 시 API 지연, DB 지연, 파서 지연, UI 지연을 각각 기록한다. 사용자가 보고한 3일/2%만으로 전체 완료 시간을 추정하지 않는다.

## 3. 목표 구조: 목록과 본문 분석을 독립 실행

```text
구글 계정 / 드라이브 선택
          │
          ▼
메타데이터 목록 수집 ──▶ 페이지 단위 DB 저장 ──▶ 즉시 목록·검색 제공
          │                      ▲
          └─ 체크포인트 저장      │
                                 │
변경분 동기화 ───────────────────┘

사용자가 본문 검색 활성화 / 파일 미리보기 요청
          │
          ▼
별도 작업 큐 ──▶ 필요한 파일 다운로드 ──▶ 텍스트 추출 / 선택적 OCR
                                               │
                                               ▼
                                       본문 검색 색인 갱신
```

목록 수집기는 파서 실행을 기다리지 않는다. 본문 분석이 실패하거나 꺼져 있어도 파일명 검색·필터·원본 열기는 작동해야 한다. 기본 연결 옵션은 ‘목록만 빠르게 가져오기’로 둔다.

### 연결 방식에 따른 구현

- 현재 확인한 로컬 가상 드라이브 순회 기능에 Drive API 커넥터를 추가한다. 로컬 경로에서 원격 파일 ID를 항상 복원할 수 있다고 가정하지 않고, 계정 연결 후 API 기반 목록을 따로 구축한다. 동일 파일이 로컬과 API 소스에 중복 노출되는 경우도 원격 ID 매핑이 검증된 범위에서만 통합한다.
- 신설 커넥터는 `src-tauri/src/connectors/google_drive/`를 제안 위치로 삼고 계정 인증·범위 열거·페이지 수집·변경 동기화를 나눈다. 기존 `application/commands.rs`, `domain/models.rs`, `src/lib/ipc.ts`, `src/lib/types.ts`, `FolderSidebar.tsx`에 소스 선택과 상태 계약을 확장한다. 이 경로는 새 모듈의 제안이며 아직 생성하지 않았다.
- OAuth 데스크톱 연결, 토큰 갱신·안전한 저장, 전체 메타데이터 접근에 맞는 범위 검토, 계정 연결 해제를 포함한다. 앱에서 선택한 파일만 접근하는 권한으로 계정 전체를 열거할 수 있다고 가정하지 않는다. 공개 배포 시 인증 검증 필요 여부와 준비 기간은 구현 착수 때 확인한다.
- 계정 연결을 사용할 수 없는 환경의 로컬 탐색 모드는 유지하되 클라우드 전체 목록의 완전성과 같은 성능을 보장하지 않는다. 선택한 동기화 폴더 밖의 파일은 로컬 탐색만으로 확인할 수 없다.

## 4. 구글 드라이브 대규모 목록 수집

### 4.1 메타데이터만 요청

`files.list`와 `fields`를 사용해 필요한 필드만 받는다. 기본 필드는 `id`, `name`, `mimeType`, `parents`, `driveId`, `modifiedTime`, `size`, `trashed`, `version`, `shortcutDetails`로 설계하고 사용하지 않는 필드는 제외한다. `size` 등 일부 필드는 없을 수 있으므로 0바이트로 오해하지 않게 처리한다.

목록 단계에서는 PDF 변환, Google 문서 내보내기, OCR, 파일 전체 해시 계산, 썸네일 일괄 다운로드를 하지 않는다. 원본 열기 URL·미리보기 등은 필요한 시점에 보완한다. `fields`로 응답량을 줄이는 근거는 [Google 성능 가이드](https://developers.google.com/workspace/drive/api/guides/performance)를 따른다.

### 4.2 페이지와 조회 범위

`pageSize=1000`을 초기 후보로 사용하고 실제 응답 크기·지연을 측정한다. 요청보다 적은 결과를 받아도 `nextPageToken`이 있으면 계속한다. 한 조회 범위의 다음 페이지는 앞선 응답 토큰에 의존하므로 무작정 병렬화하지 않는다. 독립적인 공유 드라이브 사이에서만 제한적으로 병렬 수집한다.

개인 영역과 공유 드라이브의 범위를 명시하고, 공유 드라이브는 `corpora=drive`와 `driveId` 등 지원 옵션을 맞춘다. `allDrives` 한 번의 검색으로 완전성을 단정하지 않는다. `incompleteSearch`가 참이면 완료로 표시하지 않고 범위를 나누어 보완한다. 파일 수집은 가능한 범위별 평면 목록 조회를 사용하고, 폴더 트리는 부모 ID로 구성한다. 특정 폴더만 선택하는 기능은 별도 탐색 범위로 구현한다.

목록 토큰이 거부되면 해당 범위를 처음부터 다시 열거하되 기존 DB를 삭제하지 않고 ID로 중복 갱신한다. 위 동작의 API 근거: [files.list 공식 명세](https://developers.google.com/workspace/drive/api/reference/rest/v3/files/list).

### 4.3 누락과 중복 방지

- 기본키는 공급자·계정·원격 파일 ID 조합으로 둔다. 이름이나 경로는 변경될 수 있으므로 식별자로 쓰지 않는다.
- 내 드라이브, 공유받은 파일, 공유 드라이브, 휴지통 포함 여부를 제품에서 구분한다. 계정 권한으로 볼 수 없는 파일까지 전체 검색을 보장하지 않는다.
- 부모 폴더가 아직 수집되지 않았다면 ‘위치 확인 중’으로 표시한다. 위치가 없다는 이유로 파일을 제외하지 않는다.
- 바로가기와 대상 파일을 구별하고 대상 접근 불가·순환 참조를 처리한다. 바로가기 때문에 원본을 반복 수집하지 않는다.
- 부분 수집이나 오류 종료만으로 기존 파일을 삭제 처리하지 않는다. 완료된 조회 범위와 변경 이벤트를 근거로 정리한다.

### 4.4 이어받기와 변경분 동기화

페이지 데이터와 다음 토큰은 하나의 DB 트랜잭션으로 저장한다. 앱이 중간에 종료되면 저장된 페이지 이후부터 재개하고, 페이지를 다시 받아도 중복 행이 생기지 않게 한다.

초기 열거 전에 변경 시작 토큰을 확보하고, 열거 후 그 시점부터 변경분을 재생하여 수집 중 추가·이동·삭제를 반영한다. 이후에는 `changes.list` 기반으로 갱신한다. 범위별 토큰과 적용 결과를 함께 저장하고 같은 변경을 여러 번 적용해도 결과가 같게 만든다. 변경 추적 API 근거: [Retrieve changes](https://developers.google.com/workspace/drive/api/guides/manage-changes).

토큰 무효·조회 범위 변경·계정 재연결은 재동기화 사유로 기록한다. 안정된 테스트 데이터에서는 정확한 일치를 검증하고, 실제 드라이브에서는 변경분 적용 완료 시점과 마지막 동기화 시간을 표시한다.

### 4.5 속도 제한과 장애 처리

네트워크 오류·일시적 서버 오류·요청 제한은 오류 종류에 맞게 지수 백오프와 임의 지연을 적용한다. 403은 요청 제한인지 접근 거부인지 구분하고 영구 접근 거부는 반복하지 않는다. 계정 인증 만료는 재인증 상태로 전환한다.

계정·프로젝트 제한에 맞춰 동시 요청을 조절하고 재시도 횟수와 대기 시간을 화면에서 확인할 수 있게 한다. 고정된 할당량을 전제로 속도를 약속하지 않는다. [Google 사용량 제한 가이드](https://developers.google.com/workspace/drive/api/guides/limits)를 구현 시점에 재확인한다.

## 5. 저장소·검색·본문 처리 개선

### 메타데이터 저장과 검색

현재 SQLCipher/SQLite/FTS5를 유지하는 안을 우선한다. 새로운 DB 제품 도입보다 FTS 행 식별, 쿼리 계획, 쓰기 묶음 크기, 읽기 연결 분리부터 개선한다. 기존 `documents`, `document_content`, `index_jobs`, `index_job_files`와 복구 테이블을 확장하고, 아래 논리 구조를 무조건 중복 테이블로 만들지 않는다.

논리 데이터 구조는 다음과 같다.

| 데이터 | 주요 항목 | 목적 |
|---|---|---|
| sources | 계정, 조회 범위, 연결 상태 | 소스별 상태 분리 |
| files | 원격 ID, 이름, 부모, 종류, 크기, 수정 시각, 버전 | 목록·필터·정렬 |
| sync_checkpoints | 소스, 목록 토큰, 변경 토큰, 실행 세대, 저장 시각 | 이어받기·복구 |
| content_jobs | 파일 ID, 버전, 상태, 시도 횟수, 오류, 우선순위 | 본문 분석 독립 실행 |
| content_index | 파일 ID, 분석 버전, 추출 텍스트 | 선택적 본문 검색 |

이름·부모·종류·수정일 등 실제 조회에 맞춰 인덱스를 구성한다. 깊은 페이지는 대량 OFFSET 대신 안정된 정렬 키와 ID를 이용한 커서 조회를 검토한다. 한국어·영문·숫자·확장자·부분 문자열 검색을 구분해 검증하고, 단순 전체 테이블 순회가 필요한 검색은 별도 색인 설계로 보완한다.

수집 중 정렬 순서가 바뀌어 선택한 파일이 사라지거나 스크롤이 튀지 않게 ID로 선택 상태를 유지한다. 전체 건수와 필터별 집계도 매 입력마다 전체 DB를 다시 세지 않게 한다.

### 본문 분석

기존 파서·OCR는 이미 별도 클라이언트이며 `lib.rs`에서 파서 30초, OCR 300초 제한을 지정한다. 이를 유지하되 목록과의 자원 경쟁, 원격 파일 열기 단계, 취소와 재시도의 실효성을 추가 검증한다.

- 기본 비활성화 또는 선택 범위만 실행하며, 사용자가 요청한 파일·폴더를 우선 처리한다.
- 텍스트 PDF는 텍스트 추출을 먼저 시도하고, 이미지 PDF는 OCR 옵션에 따라 처리한다.
- 파일 크기·페이지 수·처리 시간·메모리 상한을 두고 손상·암호화 파일은 사유를 표시한다.
- 작업자는 별도 프로세스 또는 현재 스택에 적합한 격리 방식으로 실행해 파서 정지가 UI를 멈추지 않게 한다.
- 파일 버전과 파서 버전이 같으면 재분석을 생략하고, 변경된 파일만 재처리한다.
- 콘텐츠 큐에 수백만 작업을 메모리로 쌓지 않고 DB에서 제한된 묶음으로 가져온다. 대기열과 다운로드 캐시 크기도 제한한다.
- 외부 AI 전송은 목록 기능의 전제 조건으로 만들지 않는다. 도입 시 사용자가 실행 범위와 전송 여부를 알 수 있게 한다.

## 6. UI/UX 개편안

### 화면 구성

```text
┌──────────────────────────────────────────────────────────────────┐
│ EveryFile   [ 파일명 검색                         ]   작업  설정 │
├─────────────┬──────────────────────────────────┬─────────────────┤
│ 전체 파일   │ 소스: 전체  종류: 전체  수정일    │ 선택 파일 정보  │
│ 로컬        ├──────────────────────────────────┤ 이름 / 위치     │
│ Google Drive│ 이름        위치     크기  수정일 │ 원본 열기       │
│ 공유 드라이브│ … 화면에 보이는 행만 표시 …     │ 경로 복사       │
│ 즐겨찾기    │                                  │ 미리보기        │
│ 저장한 검색 │                                  │ 본문 분석 요청  │
├─────────────┴──────────────────────────────────┴─────────────────┤
│ 목록 128,400개 발견 · 수집 중 · 850개/초    [일시정지] [상세]     │
└──────────────────────────────────────────────────────────────────┘
```

위 숫자는 화면 설명용 예시이며 현재 성능 측정값이 아니다.

### 디자인과 사용성 원칙

기존 앱은 이미 `src/styles/tokens.css` 기반 스타일, 좌우 패널, 북마크·태그·스마트 폴더·통계·내보내기·AI 옵션을 갖고 있다. 이 기능들을 신설로 계산하지 않고 현재 컴포넌트를 재사용해 검색 중심 화면과 상태 표현을 개선한다. 화면 평가는 소스 기준 제안이며 실제 실행 화면의 디자인 검수는 구현 단계에서 수행한다.

- 차분한 중립색, 한 가지 강조색, 일관된 아이콘과 여백을 사용한다. 밝은 테마·어두운 테마·시스템 테마를 지원한다.
- 파일 목록은 밀도 ‘기본/촘촘하게’, 열 크기 조절, 정렬, 열 표시 설정을 제공한다.
- 검색을 상단에 고정하고 소스·확장자·종류·수정일·크기 필터를 칩으로 표시한다. 적용된 필터와 한 번에 해제하는 동작을 분명하게 한다.
- 오른쪽 상세 패널은 접을 수 있게 한다. 좁은 화면에서는 선택 파일 상세 화면으로 전환한다.
- 키보드 검색 이동, 방향키 탐색, Enter로 열기, 선택 유지, 명확한 포커스, 고배율 화면과 긴 한국어 파일명을 지원한다.
- 전체 데이터를 화면에 넣지 않고 보이는 행만 렌더링한다. 애니메이션은 짧고 절제하며 모션 감소 설정을 따른다.

### 진행 상태를 다시 설계

‘전체 2%’ 하나로 표현하지 않고 다음 상태를 분리한다.

| 상태 | 사용자에게 보여줄 내용 |
|---|---|
| 최초 연결 | 인증 상태, 가져올 범위, 목록만/본문 분석 옵션 |
| 목록 수집 중 | 발견 건수, 최근 처리 속도, 마지막 성공 시각, 즉시 검색 가능한 목록 |
| 전체 건수 미확정 | ‘전체 개수 확인 중’; 확정되지 않은 백분율은 표시하지 않음 |
| 본문 분석 중 | 분석 대상·완료·실패·제외 건수, 일시정지와 선택 범위 |
| 요청 제한 대기 | 대기 이유, 다음 재시도 시각, 이미 수집된 목록 유지 |
| 권한·네트워크 오류 | 영향받은 소스, 다시 연결/재시도, 오류 상세 |
| 수집 완료 | 범위별 완료 여부, 마지막 동기화 시각, 본문 분석은 별도 상태 |

‘검색 결과 없음’과 ‘아직 목록 수집 중이라 찾지 못했을 수 있음’을 구별한다. 처리 속도 표본이 부족하면 남은 시간을 표시하지 않는다.

## 7. 더 유용한 기능의 우선순위

| 순서 | 기능 | 이유 |
|---|---|---|
| 필수 | 로컬·클라우드 통합 파일명 검색 | 본문 분석 없이 즉시 가치 제공 |
| 필수 | 원본 열기, 위치 확인, 경로·링크 복사 | 찾은 파일을 바로 사용 |
| 필수 | 소스별 수집 일시정지·재개·실패 재시도 | 오래 걸리는 작업을 통제 |
| 다음 | 저장한 검색, 즐겨찾기, 최근 사용 | 반복 탐색 시간 단축 |
| 다음 | 선택 파일 미리보기·선택 폴더 본문 검색 | 분석 자원을 필요한 곳에 사용 |
| 다음 | CSV 목록 내보내기 | 수집 결과를 앱 밖에서도 활용; 대용량은 스트리밍 출력 |
| 이후 | 중복 후보 탐색, 자연어 검색, 요약 | 목록·정확성·안정성 확보 후 추가 |

이 표는 도입 또는 개선 우선순위다. 기존 북마크·스마트 폴더·미리보기·내보내기·AI 기능은 클라우드 소스 대응과 사용성 개선 대상으로 분류하며 재구현하지 않는다.

중복 후보를 찾기 위해 초기 수집 시 모든 원본을 다운로드하지 않는다. 이름·크기 등 메타데이터 비교는 후보 표시로 한정하고 확정 비교는 사용자가 선택한 범위에서 수행한다.

## 8. 성능 목표와 측정 방법

다음은 개발 수용 목표다. 기준 PC, SSD, 메모리, 앱 버전, 네트워크, 계정 권한, API 제한, 파일 구성을 기록한 뒤 확정한다.

| 항목 | 초기 목표 | 측정 조건 |
|---|---|---|
| 기존 캐시 첫 목록 | 1초 이내 | 100만 건 DB, 앱 준비 후 조회, p95 |
| 신규 연결 첫 목록 | 인증 완료 후 3초 이내를 목표 | 정상 네트워크, API 지연 별도 기록, p95 |
| 파일명 검색·필터 | 300ms 이내를 목표 | 100만 건, 합의된 검색 유형, p95 |
| 사용자 입력 반응 | 100ms 이내를 목표 | 수집·본문 분석 동시 실행 |
| 화면 스크롤 | 60Hz 화면에서 60fps 지향 | 100만 건, 가상 목록, 긴 이름 포함 |
| 목록 단계 콘텐츠 읽기 | 0회 | 다운로드·내보내기·PDF/OCR 호출 계측 |
| 재개 정확성 | 중복 0건, 최종 누락 0건 | 고정된 정답 데이터·강제 종료 시나리오 |
| 메모리 사용 | 전체 파일 수에 비례한 행 객체 적재 없음 | 10만→100만→300만 건 비교, 수치 예산은 기준 측정 후 확정 |

### 전체 목록 완료 시간은 별도로 평가

최대 1,000건을 매 페이지 받는 이상적인 경우, 100만 파일도 최소 약 1,000회 목록 요청이 필요하다. 페이지당 네트워크+저장 시간이 평균 0.5~2초라면 단일 연속 페이지 흐름의 단순 계산은 약 8~33분이다. 300만 파일이면 약 25~100분이다.

이는 **실측이나 보장이 아닌 산술 예시**다. 짧은 페이지, 요청 제한, 재시도, 드라이브 구성, 동시 변경 때문에 실제 시간은 늘어날 수 있다. 초기 목표는 1만·10만 건 실제 메타데이터 측정으로 정하고, 수백만 건의 전체 수집이 끝나기 전부터 목록을 사용하는 경험을 보장하는 데 집중한다.

## 9. 구현 단계와 산출물

| 단계 | 작업 | 산출물 / 통과 조건 | 예상 작업량 |
|---|---|---|---|
| 0. 현황 진단 | 프로젝트 지침·소스·DB·로그·실행 화면 검토, 병목 계측 | 호출 흐름, 실제 수정 대상 파일, 단계별 성능 보고서 | 1~2일 |
| 1. 긴급 개선 | 목록과 파싱 분리, 페이지 저장, 부분 목록 표시, 진행 상태 수정 | 목록만 모드 및 파서 호출 0회 검증 | 2~4일 |
| 2. 완전성·복구 | 범위별 열거, 체크포인트, 재시도, 변경분 동기화 | 누락·중복·재시작·권한 변경 시나리오 통과 | 3~5일 |
| 3. 검색·UI | DB 조회 개선, 가상 목록, 통합 검색, 상세 패널·상태 화면 | 100만 건 성능 측정과 실제 화면 검수 | 4~7일 |
| 4. 본문 분석 | 독립 작업자, 우선순위, 파서 제한, 실패 격리 | 분석 중에도 목록·검색 성능 유지 | 3~5일 |
| 5. 배포 검증 | 300만 건·장시간·마이그레이션·회귀 검증 | 설치 가능 빌드, 변경 내역, 복구 절차 | 2~4일 |

합계 약 15~27 작업일은 기존 로컬 경로 개선 중심의 잠정 추정이며 약속 일정이 아니다. 현재 없는 Drive API 커넥터·OAuth·클라우드 원본 열기까지 포함하면 추가 5~10 작업일을 예비 범위로 두고, 공개 배포 인증 검증 대기는 별도로 잡는다. 단계 0 이후 재산정한다. 단계 1에서는 진행 상태·목록 전용 모드·실측된 DB 병목의 작은 개선판을 먼저 제공하고, API 수집 정확성을 확보한 뒤 UI와 고급 기능을 순차 배포한다.

## 10. 검증 계획

1. **원인 재현:** 현재 ‘3일/2%’ 상태의 로그와 카운터 의미 확인. 목록만, 다운로드 포함, 파싱 포함 실행을 같은 작은 범위에서 비교한다.
2. **목록 계약 검사:** PDF·이미지·Google 문서가 섞여 있어도 목록 실행에서 원본 읽기가 발생하지 않는지 검증한다.
3. **완전성 검사:** 페이지 축소·빈 페이지와 다음 토큰·토큰 거부·공유 드라이브·부모 지연·동명이인 파일·바로가기·부분 실패를 모의 응답으로 검증한다.
4. **복구 검사:** API 응답 직후, DB 커밋 전후, 토큰 저장 시점에 종료한 후 재개해 최종 결과를 정답과 대조한다.
5. **변경 검사:** 수집 중 생성·이동·이름 변경·삭제·권한 상실을 발생시키고 변경 추적 후 수렴하는지 확인한다.
6. **대량 검사:** 합성 10만·100만·300만 건으로 DB/UI를 검증하고, 실제 계정의 허용된 범위에서 API 처리량을 측정한다. 합성 결과를 실제 Drive 속도로 보고하지 않는다.
7. **파서 격리 검사:** 손상·암호화·대형·이미지 PDF와 시간 초과가 다른 작업에 영향을 주지 않는지 확인한다.
8. **회귀·화면 검사:** 기존 로컬 검색·열기·필터·설정·계정 연결을 점검하고 고배율·작은 화면·키보드 사용·긴 파일명·수집 중 스크롤을 실제 렌더링 화면에서 검수한다.
9. **장시간 검사:** 최소 24시간 동기화에서 메모리 증가·큐 적체·DB 잠금·중복 재시도와 재시작 후 상태를 확인한다.

## 11. 기존 데이터 보호와 배포

### L드라이브 사용 원칙 — 사용자 지정 필수 조건

- 앞으로 새로 설치하는 도구·의존성·모델·다운로드·작업 임시 파일·빌드 산출물은 L드라이브에 둔다. 이번 계획서 작업에서는 추가 설치나 모델 다운로드를 실행하지 않았다.
- 프로젝트 내 작업 경로는 `L:\codex-L\EveryFile`로 유지한다. 작업용 캐시는 해당 폴더 아래 `.cache`, `.tmp`, `artifacts` 등 역할별 위치를 사용한다.
- 설치·빌드 전에 npm 캐시, Python 가상환경·pip 캐시, Rust 산출물 경로(`CARGO_TARGET_DIR`), OCR 모델 캐시와 자식 프로세스의 `TEMP`/`TMP`를 L드라이브로 지정하고 실제 적용 경로를 확인한다. 기존 도구 저장소를 전역 변경하거나 C드라이브의 설치를 임의로 이동·삭제하지 않는다.
- 앱의 대용량 DB·로그·콘텐츠 캐시도 L드라이브를 선택할 수 있도록 `lib.rs` 초기화 경로와 설정 화면을 개선한다. 현재 기본 AppData 경로를 그대로 둔 채 ‘L에 설치했으니 해결’로 판단하지 않는다.
- 데이터 이동은 앱 쓰기를 멈춘 상태에서 DB·암호화 키·WAL 일관성을 보장하는 백업/복사 후 검증하고, 성공 확인 전 기존 데이터를 삭제하지 않는다. C 공간 부족을 백업 때문에 악화시키지 않도록 백업 대상도 L로 둔다.
- L드라이브 여유 공간이 부족하거나 연결되지 않으면 C로 자동 우회하지 않고 원인과 필요한 공간을 표시한다. OS가 관리하는 불가피한 시스템 기록까지 앱이 완전히 통제한다고 약속하지 않으며, C에 실질적 설치 공간이 필요한 도구는 실행 전에 대안을 확인한다.

### 마이그레이션과 복구

현재 작업·DB·로그를 확인하고 변경 전 백업을 만든다. DB 스키마는 버전을 부여하고, 기존 목록을 보존한 채 새 메타데이터 수집기를 시험한다. 기존 파싱 결과는 원격 ID·버전 매칭이 검증되는 범위에서 재사용한다.

새 수집기와 본문 분석 분리 기능은 기능 플래그로 점진 적용한다. 마이그레이션 실패 시 기존 버전과 백업으로 돌아가는 절차를 검증한다. 원격 원본 파일의 이동·삭제·권한 변경은 이번 개선에 필요하지 않다.

로그에는 처리 시간, 건수, 오류 코드, 재시도, 큐 길이를 기록하되 OAuth 토큰·문서 본문은 남기지 않는다. 진단 자료에는 파일명 등 민감한 식별 정보를 줄인다. 인증정보는 현재 플랫폼의 안전한 저장소를 활용하도록 점검한다.

## 12. 구현 완료 판단 기준

- 드라이브를 연결하면 본문 분석을 기다리지 않고 첫 목록이 나타나고 검색할 수 있다.
- 100만·300만 건에서 화면과 메모리가 전체 데이터의 일괄 적재 때문에 무너지지 않는다.
- 목록 수집 완료와 본문 분석 완료가 분리되고, 전체 건수 미확정 상태에서 오해를 부르는 백분율이 나오지 않는다.
- 중단·재시작·요청 제한·개별 파일 실패 이후에도 저장한 결과를 유지하며 복구한다.
- 공유 드라이브와 권한 범위를 표시하고, 누락 가능성이 있는 상태를 완료로 오인시키지 않는다.
- 합의한 검색 성능과 정확성 검사를 통과하며 기존 로컬 기능도 유지한다.
- 최종 인수 자료에 실제 변경 파일, 원인과 수정 내용, 전후 측정치, 검증 결과, 알려진 제한, 롤백 방법을 포함한다.

**권장 착수 범위는 단계 0~2다.** 가장 큰 불편인 목록 지연·부정확성·복구 문제를 먼저 해결하고, 이 구조 위에 검색과 UI를 개선하면 후속 기능도 안정적으로 확장할 수 있다.
